def format_crate(package, check=False):
    """
    Runs `rustfmt --edition ${package.edition} [--check] ${target.src_path}` for each target
    in ${package}.
    """
    fail(msg="TODO define rustfmt")
